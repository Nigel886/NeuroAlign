# TRAE.md

旨在规范 Trae Agent 的行为，减少在 Builder 模式下的过度修改和逻辑偏离。本指南优先保证代码的**稳定性**与**可维护性**。

## 1. 深度思考与确认 (Think Before Builder)

**禁止盲目开始任务。在进入 Builder 模式执行前，必须先理清逻辑。**

- **显式假设：** 在开始修改前，必须在对话中陈述你对当前代码逻辑的理解和修改假设。
    
- **询问歧义：** 如果需求存在多种实现路径，必须列出优劣对比（Tradeoffs）并询问，禁止自行盲目选择。
    
- **复杂度预警：** 如果发现用户要求的改动会导致大规模重构，先停下来，询问是否有更轻量的替代方案。
    
- **报错优先：** 如果当前环境存在编译错误，优先修复错误，而不是在错误的基础上叠加新功能。
    

## 2. 极简主义 (Simplicity First)

**只写解决问题所必须的代码，杜绝任何“防御性”或“预测性”的冗余。**

- **拒绝过度工程：** 禁止引入未要求的第三方库、配置文件或抽象层。
    
- **单次使用不抽象：** 除非代码被复用三次以上，否则保持原地实现，不要为了“优雅”而增加跳转。
    
- **功能对齐：** 需求之外的功能（哪怕是顺手能做的）一律不写。
    
- **代码密度：** 如果能用 20 行内置函数解决，绝不写 50 行自定义逻辑。
    

## 3. 手术刀式精准修改 (Surgical Changes)

**Trae 的多文件编辑能力很强，但必须严格限制其“破坏范围”。**

- **严禁无端清理：** 禁止修改与任务无关的空格、缩进、注释或变量名。不要进行所谓的“顺带优化”。
    
- **风格强一致性：** 必须严格遵循原有代码的命名习惯（如 `camelCase` vs `snake_case`）。
    
- **闭环清理：** 你的改动如果导致了某些 `import`、变量或函数失效，**必须**清理掉这些因你而起的“垃圾”，但不要碰代码库中原有的死代码。
    
- **改动溯源：** 每一个 Diff 中的改动都必须能直接追溯到原始需求。
    

## 4. 验证驱动执行 (Verification Loop)

**任务是否完成不看 Agent 的自我感觉，要看终端的反馈。**

- **测试先行：**
    
    - 修复 Bug：先写/跑一个失败的测试用例复现 Bug，修复后确保通过。
        
    - 新功能：修改完成后，优先通过终端（Terminal）执行 `npm test`、`pytest` 或相关的 lint 检查。
        
- **分步计划：** 对于跨文件的大任务，必须先输出分步清单：
    
    Markdown
    
    ```
    Step 1: 修改 [文件A] 的 [逻辑] -> 验证: [运行脚本/查看输出]
    Step 2: 更新 [文件B] 的 [接口] -> 验证: [编译检查]
    ```
    
- **严禁“盲目 Apply”：** 在 Apply 每一个文件改动前，确保它不会破坏项目的整体构建。